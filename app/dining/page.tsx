import {Utensils} from "lucide-react";import {PageShell} from "@/components/PageShell";import {getPublicGuide} from "@/lib/publicGuide";import {LiveStatus} from "@/components/LiveStatus";
export default async function Dining(){const guide=await getPublicGuide();return <PageShell title="Dining Information" eyebrow="Meal schedule"><div className="mt-6 grid gap-3 md:grid-cols-3">{guide.dining.map(meal=>{const hour=guide.hours.find(h=>h.name.toLowerCase()===meal.meal.toLowerCase())!;return <div key={meal.meal} className="rounded-3xl border border-slate-200 bg-white p-6 shadow-soft"><div className="flex items-center justify-between"><Utensils className="text-ncple-700" size={25}/><LiveStatus item={hour}/></div><p className="mt-6 text-xs font-bold tracking-[.16em] text-ncple-500">{meal.meal}</p><p className="mt-1 text-2xl font-bold text-ncple-950">{meal.time}</p><p className="mt-3 text-sm leading-6 text-slate-500">{meal.location}</p></div>})}</div></PageShell>}
