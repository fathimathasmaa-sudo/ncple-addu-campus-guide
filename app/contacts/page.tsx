import {Phone} from "lucide-react";import {PageShell} from "@/components/PageShell";import {guide} from "@/data/campusGuide";
export default function Contacts(){return <PageShell title="Important Contacts" eyebrow="Get help quickly"><div className="mt-6 grid gap-3 md:grid-cols-2">{guide.contacts.map(c=><div key={c.category+c.name} className={`rounded-2xl border p-5 shadow-soft ${c.emergency?'border-ncple-500 bg-ncple-50':'border-slate-200 bg-white'}`}><p className="text-xs font-bold uppercase tracking-[.14em] text-ncple-500">{c.category}</p><p className="mt-2 font-semibold text-slate-900">{c.name}</p>{c.phone.startsWith('+')?<a href={`tel:${c.phone}`} className="mt-3 inline-flex items-center gap-2 text-sm font-semibold text-ncple-700"><Phone size={16}/>{c.phone}</a>:<p className="mt-3 text-sm text-slate-500">{c.phone}</p>}</div>)}</div></PageShell>}
