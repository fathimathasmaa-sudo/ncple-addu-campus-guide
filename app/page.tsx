"use client";
import Link from "next/link";import {useMemo} from "react";import {ArrowRight,Clock3,Map,Utensils,BedDouble,Waves,Gamepad2,Wifi,ShieldCheck,Phone,ClipboardCheck,Search} from "lucide-react";import {guide} from "@/data/campusGuide";import {getHourStatus} from "@/lib/hours";
const quick=[['/map','Campus Map',Map],['/dining','Dining',Utensils],['/accommodation','Accommodation',BedDouble],['/pool','Swimming Pool',Waves],['/entertainment','Entertainment Room',Gamepad2],['/wifi','Wi-Fi',Wifi],['/safety','Safety',ShieldCheck],['/contacts','Important Contacts',Phone],['/checkout','Checkout',ClipboardCheck]] as const;
export default function Home(){const hours=useMemo(()=>guide.hours.filter(x=>x.featured),[]);return <main className="mx-auto max-w-6xl px-4 py-5 md:px-8 md:py-10"><header className="rounded-[2rem] bg-ncple-900 p-6 text-white shadow-soft md:p-10"><div className="flex items-center justify-between"><div><p className="text-xs font-bold tracking-[.14em] text-blue-100 md:text-sm">NATIONAL COLLEGE OF POLICING AND LAW ENFORCEMENT</p><p className="mt-1 text-sm text-blue-100">Addu Campus</p></div><div className="rounded-xl border border-white/15 px-3 py-2 text-xs font-bold">NCPLE</div></div><h1 className="mt-10 max-w-3xl text-3xl font-bold tracking-tight md:text-5xl">Welcome to NCPLE Addu Campus</h1><p className="mt-4 max-w-2xl text-base leading-7 text-blue-50 md:text-lg">Your digital guide to a comfortable, safe and enjoyable stay.</p><Link href="/search" className="mt-8 flex max-w-xl items-center gap-3 rounded-2xl bg-white p-4 text-slate-600 shadow"><Search size={20}/><span className="flex-1 text-sm">Search the campus guide…</span><ArrowRight size={18}/></Link></header><section className="mt-10"><div><p className="text-xs font-bold uppercase tracking-[.16em] text-ncple-500">Quick Access</p><h2 className="mt-1 text-2xl font-bold text-ncple-950">Find what you need</h2></div><div className="mt-4 grid grid-cols-2 gap-3 sm:grid-cols-3 lg:grid-cols-5">{quick.map(([href,label,Icon])=><Link href={href} key={href} className="group rounded-2xl border border-slate-200 bg-white p-4 shadow-soft transition hover:-translate-y-0.5 hover:border-ncple-500"><div className="flex h-11 w-11 items-center justify-center rounded-xl bg-ncple-50 text-ncple-700"><Icon size={21}/></div><div className="mt-4 flex items-center justify-between gap-2"><span className="text-sm font-semibold text-slate-800">{label}</span><ArrowRight size={16} className="text-slate-400"/></div></Link>)}</div></section><section className="mt-10"><div className="flex items-center gap-2"><Clock3 size={20} className="text-ncple-700"/><div><p className="text-xs font-bold uppercase tracking-[.16em] text-ncple-500">Today at Campus</p><h2 className="text-2xl font-bold text-ncple-950">Hours at a glance</h2></div></div><div className="mt-4 grid gap-3 md:grid-cols-2 xl:grid-cols-3">{hours.map(item=>{const s=getHourStatus(item);return <div key={item.id} className="rounded-2xl border border-slate-200 bg-white p-5 shadow-soft"><div className="flex items-start justify-between gap-3"><div><p className="font-semibold text-slate-900">{item.name}</p><p className="mt-1 text-sm text-slate-500">{item.timeLabel}</p></div><span className={`rounded-full px-2.5 py-1 text-xs font-semibold ${s.isOpen?'bg-emerald-50 text-emerald-700':'bg-slate-100 text-slate-600'}`}>{s.label}</span></div></div>})}</div></section></main>}
