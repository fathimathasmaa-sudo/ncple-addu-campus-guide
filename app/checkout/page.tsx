"use client";
import {useEffect,useState} from "react";import {Check,Circle} from "lucide-react";import {PageShell} from "@/components/PageShell";import {guide} from "@/data/campusGuide";
const KEY="ncple-checkout-progress";
export default function Checkout(){const [done,setDone]=useState<boolean[]>(guide.checkout.map(()=>false));useEffect(()=>{try{const saved=localStorage.getItem(KEY);if(saved){const parsed=JSON.parse(saved);if(Array.isArray(parsed)&&parsed.length===guide.checkout.length)setDone(parsed)}}catch{}},[]);const completed=done.filter(Boolean).length;function toggle(i:number){setDone(prev=>{const next=prev.map((v,n)=>n===i?!v:v);localStorage.setItem(KEY,JSON.stringify(next));return next})}return <PageShell title="Before You Check Out" eyebrow="Departure checklist"><div className="mt-6 rounded-3xl bg-white p-6 shadow-soft"><div className="flex items-center justify-between gap-4"><div><p className="text-sm text-slate-500">Progress</p><p className="text-2xl font-bold text-ncple-950">{completed} of {guide.checkout.length} completed</p></div><div className="h-2 w-32 overflow-hidden rounded-full bg-slate-100"><div className="h-full bg-ncple-700 transition-all" style={{width:`${completed/guide.checkout.length*100}%`}}/></div></div></div><div className="mt-5 grid gap-3">{guide.checkout.map((x,i)=><button key={x} onClick={()=>toggle(i)} className="flex w-full items-start gap-4 rounded-2xl border border-slate-200 bg-white p-5 text-left shadow-soft">{done[i]?<span className="flex h-6 w-6 shrink-0 items-center justify-center rounded-full bg-ncple-700 text-white"><Check size={15}/></span>:<Circle className="shrink-0 text-slate-300" size={24}/>}<span className={`text-sm leading-6 ${done[i]?'text-slate-400 line-through':'text-slate-700'}`}>{x}</span></button>)}</div>{completed===guide.checkout.length&&<div className="mt-5 rounded-2xl bg-emerald-50 p-5 text-sm font-semibold text-emerald-800">Checklist complete.</div>}</PageShell>}
